import Promotion from "@/screens/clients/Promotion";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Promoção",
  description: "Promoção de produtos",
};

export default Promotion;
