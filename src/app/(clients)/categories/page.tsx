import Categories from "@/screens/clients/Categories";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Categorias",
  description: "Categorias das encomendas",
};

export default Categories;
