import Perfil from "@/screens/clients/user/Perfil";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Perfil do usuário",
  description: "Perfil do usuário",
};
export default Perfil;
