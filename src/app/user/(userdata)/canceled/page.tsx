import CanceledScreen from "@/screens/clients/user/Canceled";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Encomendas canceladas",
  description: "Minhas encomendas cnaceladas",
};

export default CanceledScreen;
